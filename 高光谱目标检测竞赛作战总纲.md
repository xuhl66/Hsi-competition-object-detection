# 高光谱目标检测竞赛：作战总纲与首选技术路线

> 版本：v0.5（2026-08-08）
> 竞赛：Hyperspectral Object Detection Challenge 2026  
> 核心原则：**在完全遵守规则、结果可复现的前提下，一切技术决策只服务于最终私榜 `mAP@[0.50:0.95]`；AP50、速度和模型大小都是诊断量，不是最终目标。**
>
> **落地更新：** 本文保留最初 OSSDet-16 与 V1 HYPER-DET 路线的研究论证。
> V1 全量训练没有带来有效公榜增益后，当前主线已升级为
> **Objects365→COCO DEIM-D-FINE-X + 16 波段代理输入 + 原生光谱残差支路**。
> 实际代码、路径和运行状态以
> [最终方案实施状态.md](最终方案实施状态.md) 为准。
>
> **永久训练原则：** 战术激进、工程严谨。训练时间宝贵意味着一次把高潜力模型
> 训练到可信收敛，而不是用欠训练的 Pilot 草率淘汰后反复重跑。Smoke 只验证
> 工程链路；未收敛模型不得端给用户作架构结论。
>
> **当前迭代原则：** V5/V6/V7/V8/... 先持续提升模型能力，统一使用已经公榜
> 验证的两视图冠军推理协议。`0.70+` 是阶段里程碑而非停止上限；模型端形成可信
> 平台后才暂停更新、观察排行榜并研究额外推理优化，合规边界储备仍保持冻结。
>
> **当前落地状态：** V5/V5R 已由同协议公榜证据淘汰；冠军仍为 V4 e30。
> 当前工作位于独立 `agent/v6-independent` 分支，V6 不继承 V4/V5/V5R 权重，
> 只从公开 Co-DINO ViT-L 初始化。量化诊断、架构和独立提交契约以
> [docs/v6_diagnosis_and_design.md](docs/v6_diagnosis_and_design.md) 为准。

## 0. 一页结论

我们要训练一个**单一的 16 波段高光谱目标检测模型**，在每张图中定位并识别 18 类目标，最终输出每个目标的类别、置信度和水平边界框。

最初建议的首选路线是：

**以 AAAI 2026 的单流多光谱检测器 OSSDet 为主体，做成适配本赛 16 波段、水平框和 COCO AP 的 `OSSDet-16`。**

V1 曾进一步吸收 HYPER-DET、SSRM、3-D 光谱—空间支路和 HUFF 融合；真实
公榜证据表明仅延长其训练收益不足。当前 V2 使用更强的 Objects365→COCO
DEIM-D-FINE-X 主核，保留完整预训练空间主干，并通过可学习 16→3 代理与
原生 16 波段多尺度残差同时利用光谱信息。

选择它的主要依据不是“模型名字新”，而是它同时满足：

1. 论文任务与本赛都是多/高光谱目标检测，而非高光谱分类或显著性分割；
2. 它在 HOD3K 上直接报告本赛同款主指标，`mAP@[.50:.95] = 60.9`、`AP75 = 68.8`；
3. HOD3K 与本赛输入高度相似：约 512×256、16 个可见光波段；
4. 它是单流、单模型，天然贴合“禁止集成”的规则；
5. 论文已由 AAAI 2026 正式发表，并有可用开源代码。

这些数值**不能当成本赛预期分数**：HOD3K 与本赛不是同一数据集，类别和分布也不同。是否真的最优，只由无泄漏本地验证和 Kaggle 分数决定。

### 0.1 当前冠军基线与后续版本协议

截至 2026-07-30，当前公榜冠军控制组不是裸模型，而是：

- V4 Co-DINO ViT-L，原生 16 波段光谱融合与目标感知细化；
- `best_bbox_mAP_epoch_30.pth` 的 EMA 单 checkpoint；
- 全图 1280 identity + horizontal 两视图；
- 同类 Gaussian Soft-NMS：`iou_threshold=0.55`、`sigma=0.5`；
- `pre_score=output_score=0.0001`，每图全类别最多 300 框；
- Fold0 `mAP@[.50:.95]=0.71711748`，Kaggle 公榜 `0.67686`。

V5 及其后的 V6/V7/V8/... 使用各自充分收敛的单一 EMA checkpoint，但固定采用
上述同一推理协议。模型迭代期间不混入 Soup、SWA、full TTA、切片、WBF、逐类
阈值或测试分布校准，以确保版本差异来自模型能力。单视图只用于诊断；正式晋级和
提交比较使用完整冠军协议。

0730_b 的 e54 + 12-view + `sigma=0.7` + max500 虽在同一 Fold0 上比冠军高
`0.00620`，公榜却低 `0.00087`；0730_c 的 e30 四切片本地仅高 `0.000385`，
公榜低 `0.00004`。因此，在模型能力尚有空间时停止继续堆叠二阶后处理。

---

## 1. 官方任务到底是什么

官方入口：

- [Overview](https://www.kaggle.com/competitions/hyperspectral-object-detection-challenge-2026/overview)
- [Data](https://www.kaggle.com/competitions/hyperspectral-object-detection-challenge-2026/data)
- [Rules](https://www.kaggle.com/competitions/hyperspectral-object-detection-challenge-2026/rules)

### 1.1 任务

输入是一张由快照式光谱相机拍摄的 16 位单通道马赛克 PNG。必须先使用官方 `X2Cube` 方式解码为 `(H, W, 16)` 的光谱立方体，再预测：

- 目标类别；
- 置信度；
- 水平边界框 `(x1, y1, x2, y2)`。

相机为 **XIMEA MQ022HG-IM-SM4X4-VIS3**，4×4 光谱滤波阵列，16 个窄波段覆盖约 460–600 nm。标注中的宽高指解码后的单波段空间分辨率，不是原始马赛克 PNG 的分辨率。

### 1.2 数据

| 数据 | 数量与内容 | 发布时间 |
|---|---:|---|
| `data_train.zip` | 3,000 张图，约 11 GB；PNG + Pascal VOC XML | 2026-07-25 |
| `data_test.zip` | 1,000 张图，约 3.7 GB；无标注 | Phase 1 |
| `ranking_images.zip` | 1,000 张图；无标注 | 2026-09-23 |
| `class.txt` | 18 类及 0-based `class_id` | 随训练数据 |
| `sample_submission.csv` | 官方提交格式 | 随数据 |

18 类为：

```text
0 apple             1 apple_plastic
2 badminton         3 banana
4 banana_plastic    5 car
6 car_toy           7 charger_head
8 e-bike            9 egg
10 egg_plastic      11 egg_wood
12 orange           13 orange_plastic
14 people           15 rubik
16 stone_block      17 table_tennis
```

这里存在明显的“实物/仿制物”易混类别，如 apple/apple_plastic、banana/banana_plastic、car/car_toy、egg/egg_plastic/egg_wood、orange/orange_plastic。外观相近、材质不同，正是高光谱信息可能产生决定性增益的地方。

### 1.3 评测

主指标：

```text
mAP@[0.50:0.95]
= 在 IoU 0.50, 0.55, ..., 0.95 共 10 个阈值上的 AP 均值
```

次指标是 `mAP@0.50`，只用于参考。

这意味着：

- “大致框到”只能改善 AP50，不能保证主指标领先；
- 高 IoU 下的精确定位、置信度排序和漏检/误检控制同等重要；
- 只按 AP50 选模型，会系统性选错方案；
- 必须让本地评测实现与 Kaggle 官方评测逐项一致。

### 1.4 两阶段排名

- **Phase 1：2026-07-25 至 2026-09-23**  
  提交 1,000 张 test 的结果。公开榜只显示随机 50% test 的分数，另一半隐藏。
- **Phase 2：2026-09-23 至 2026-09-25**  
  ranking 1,000 张发布。必须在同一个 CSV 中同时提交 test + ranking 的预测。
- test 和 ranking 在最终分数中等权；ranking 全部保持私有，赛后才揭晓。
- **Phase 2 前的提交不参与最终排名。**
- Phase 2 若只交 test、不含 ranking，ranking 图像按零分处理。

官方元数据显示最终截止时间为 `2026-09-24 16:00 UTC`，即北京时间 **2026-09-25 00:00**。仍应以 Kaggle 页面届时显示的倒计时为准，并至少提前数小时完成有效提交。

### 1.5 奖项与主办方

总奖金为 5,000 元人民币：

- 第 1 名：2,500 元 + 荣誉证书；
- 第 2 名：1,500 元 + 荣誉证书；
- 第 3 名：1,000 元 + 荣誉证书；
- 另设 3 支优秀奖队伍，颁发荣誉证书。

前三名各有一位队员获得第十届成像光谱对地观测学术研讨会注册费减免，并受邀作报告。主办单位包括国际数字地球学会中国国家委员会成像光谱专业委员会、南京理工大学、深圳大学、武汉大学和剑桥大学。

---

## 2. 规则红线：分数再高，违规即归零

### 2.1 明确禁止

1. 人工伪造标注，包括对验证/测试记录进行人工预测或手工标注；
2. 攻击评测系统或任何作弊行为；
3. 使用**未声明的外部数据**进行预训练；
4. 任何形式的多模型集成，包括投票、加权融合、模型间 WBF、post-NMS fusion；
5. 队外私下共享竞赛数据或代码；
6. 多账号参赛或提交；
7. 传播、商业使用、跨渠道污染竞赛数据，或未经许可用其发表论文。

### 2.2 单模型要求

最终提交只能来自一个检测模型。为了研发而训练多个候选模型、做交叉验证是正常实验；但最终 CSV 不能融合多个模型或多个折的预测，交付的最终权重也只能对应一个可复现模型。

以下三项在规则文字中仍有解释空间，必须先在 Kaggle 公共讨论区获得主办方的书面确认：

- ImageNet、HOD3K、MODA 等外部预训练权重，怎样才算“已声明”；
- 是否允许用无标注 test/ranking 做自监督预训练或伪标签自训练；
- 同一个模型的多尺度/翻转 TTA 是否会被认定为“任何形式的集成”。

当前运营决策把同一模型、同一 checkpoint 的确定性水平翻转视为单模型推理，
并已用于公榜冠军基线；这不等于自动放开多尺度/full TTA 或其他预测融合。最终
审查前仍须保留完整单模型解释和可复现记录，并再次核对主办方规则。外部预训练
声明及任何 test/ranking 参与训练的方向仍保持冻结。合规不是保守项，而是保住
最终 AP 和名次的必要条件。

### 2.3 队伍、提交和赛后审查

- 最大队伍人数：5；
- 每人只能加入一支队伍，系列竞赛只能选择一个 track；
- 每队每天最多 3 次提交；
- 并队只能由队长操作，并受提交次数和截止时间约束；
- 并列时，较早提交者胜；
- 冠军方案要求开源；
- 赛后前 6 名须在 2026-09-27 前提交完整审查包：
  - 训练与推理源码；
  - 最终模型权重；
  - 可复现最终 CSV 的推理脚本；
  - 环境、依赖和命令完整的 README；
  - 参数量和 FLOPs。

因此，从第一天起就必须记录配置、Git 提交、随机种子、环境、数据校验值、训练日志和推理命令，不能最后再补。

### 2.4 边界提分手段储备库（仅记录，全部冻结）

当前状态：`RECORD_ONLY / FROZEN / DO_NOT_IMPLEMENT`。

以下方向仅作为后期战略储备，**现在全部不使用**：不实现代码、不生成派生数据、不启动训练、不用于推理，也不提交由其产生的 CSV。只有常规模型结构、光谱建模、训练策略、数据增强和合规单模型后处理已经出现可信平台，且需要稳住第一名时，才允许重新评估。重新启用任何一项之前，必须由用户逐项明确授权，并完成规则依据、泄漏风险、单模型约束和最终审查可复现性的专项检查。

| 储备方向 | 可能收益来源 | 当前主要风险 | 状态 |
|---|---|---|---|
| 自动化 test/ranking 伪标签自训练 | 扩充目标域监督信号 | 测试集使用边界、确认偏差 | 冻结 |
| Phase1 test 自监督预训练（空间/波段掩码重建） | 无标注目标域表征适配 | 主办方尚未明确允许 | 冻结 |
| Phase2 test+ranking 无监督域适配 | 缓解训练域与榜单域偏移 | 测试数据参与训练的合规边界 | 冻结 |
| 多教师蒸馏到单一学生 | 把多个强模型知识压入最终单模型 | 可能被解释为变相集成，审查复杂 | 冻结 |
| 已声明的公开高光谱/外部预训练 | 提升光谱表征与小样本泛化 | 外部数据声明与许可要求未澄清 | 冻结 |
| 模型辅助或人工修正训练标签 | 降低漏标、错标和框噪声 | 人工标注边界与审查成本 | 冻结 |
| test cohort/global prior 校准 | 利用测试集类别与场景整体统计 | transductive 推断边界、易过拟合公榜 | 冻结 |
| 无标注 test 统计对齐（CORAL/AdaBN 等） | 缓解传感器和批次统计漂移 | 测试时适配是否属于训练未明确 | 冻结 |
| test 背景 Copy-Paste | 学习真实目标域背景 | 测试数据进入训练、伪影和泄漏风险 | 冻结 |
| 外部基础模型自动标注后蒸馏 | 引入额外语义先验 | 外部模型/数据声明及伪标签边界 | 冻结 |
| 单模型检索/原型记忆 | 改善弱类与长尾分类 | 是否构成额外模型或外部知识库需审查 | 冻结 |
| 用真实完整提交做公榜贝叶斯调参 | 针对隐藏域优化推理超参 | 公榜过拟合且消耗每日提交额度 | 冻结 |
| 单 checkpoint 内部 MoE/专家路由 | 提高单模型容量和类别专门化 | 单模型定义、显存和训练稳定性 | 冻结 |
| Episodic TTT / Continual TTA | 测试时适应样本或数据流 | 测试时训练与跨样本状态的合规边界 | 冻结 |
| 重复样本/采集批次分析 | 诊断分布和潜在泄漏 | 只能审计；标签迁移风险极高 | 冻结 |

以下行为不属于储备方案，任何阶段都不得采用：人工标注测试集或人工填写预测、利用评测器/CSV 解析漏洞、公榜子集探测或标签重建、多账号绕过提交限制、队外私下共享，以及把多模型预测伪装成单模型结果。

本节的“全部冻结”针对上述新增储备方向，不追溯撤销已经完成并留有审计记录的 V4 历史初始化与单 checkpoint TTA 实验。今后的启用门槛仍以本节为准，不能以“规则未明令禁止”为由自动落地。

---

## 3. 我们要做什么，最后交什么

### 3.1 研发目标

建立一条可复现流水线：

```text
原始 16-bit 马赛克 PNG
  → 官方 X2Cube 解码
  → 16 波段稳健归一化
  → 单一 OSSDet-16
  → 类别、置信度、边界框
  → 单模型后处理
  → submission.csv
```

研发过程中所有实验以同一套无泄漏验证集上的 `mAP@[.50:.95]` 排序；AP50、AP75、分类型 AP、分尺寸 AP、召回率和混淆矩阵只用于解释得失。

### 3.2 Kaggle 提交物

每个检测一行：

```csv
id,image_id,class_id,confidence,x1,y1,x2,y2
0,1000,2,0.95,186,28,201,50
1,1000,9,0.72,366,68,383,86
```

字段要求：

- `id`：全表唯一连续整数；
- `image_id`：文件名去掉 `.png`；
- `class_id`：0–17；
- `confidence`：0–1；
- 坐标必须位于官方解码后空间分辨率中。

Phase 1 交 test；Phase 2 必须重交 **test + ranking** 的完整 CSV。

### 3.3 获奖审查包

除 CSV 外，最终要保留：

```text
source/
final_model.pth
infer.py
requirements/ 或 environment.yml
README.md
训练配置与日志
参数量和 FLOPs 报告
生成最终 submission.csv 的一条命令
```

---

## 4. 为什么主指标会改变我们的打法

### 4.1 优先精确框，而非只追求“检出”

AP50 对框偏移较宽容，AP75–AP95 会迅速惩罚边界误差。本赛的实物和小物体可能只有十几到几十像素，坐标偏 1–2 像素就会显著改变 IoU。因此必须重点优化：

- 官方解码和坐标变换绝对正确；
- 高分辨率特征与小目标保留；
- IoU/质量感知分类分数；
- 边界框的初始化 + 精修；
- resize/pad 后严格逆变换并裁剪到图像边界；
- NMS 阈值、每图候选数和低分截断由 OOF 验证确定。

### 4.2 类别不平衡不能只看样本总量

标准 COCO mAP 会先按类别计算 AP 再取均值，少数类与多数类对最终均值同样重要。数据下载后的第一份统计必须包括：

- 每类图像数、实例数；
- 每类框宽高、面积、长宽比；
- 每张图实例数；
- 真实/仿制物易混对的混淆；
- 小/中/大目标占比；
- 相同背景或连续拍摄场景的聚类。

采样和损失要优先补足低 AP 类，而不是继续把已经很高的多数类推高。

### 4.3 置信度不是普通分类概率

AP依赖全数据集上的预测排序。最终分数应同时反映“类别正确”和“框质量高”，宜使用质量感知得分，而不是只用分类 softmax。任何分数融合公式都只能在 OOF 上拟合；不能根据公开榜反复手工调到过拟合。

---

## 5. 初始候选论文方案：OSSDet-16

### 5.1 论文证据

主论文：

- Han et al., **MODA: The First Challenging Benchmark for Multispectral Object Detection in Aerial Images**, AAAI 2026  
  [AAAI 正式页面](https://ojs.aaai.org/index.php/AAAI/article/view/42457) · [论文 PDF](https://ojs.aaai.org/index.php/AAAI/article/download/42457/46418) · [开源实现](https://github.com/shuaihao-han/MODA)

论文在 HOD3K 上的同设定比较：

| 方法 | AP50 | AP75 | mAP@[.50:.95] |
|---|---:|---:|---:|
| DINO | 89.1 | 61.6 | 56.3 |
| TOOD | 90.3 | 65.6 | 57.8 |
| VFNet | 92.3 | 66.5 | 59.0 |
| Cascade R-CNN | 90.9 | 66.9 | 59.6 |
| S2ADet | 93.4 | — | 59.8 |
| **OSSDet** | **93.4** | **68.8** | **60.9** |

HOD3K 有 3,242 张、约 512×256、16 波段、470–620 nm 图像；本赛是 3,000 张、约同级分辨率、16 波段、460–600 nm。传感范围和数据形式高度接近，使这项证据比普通 RGB COCO 排名更有参考价值。

另一个直接相关的新工作 SDCM 在 HOD3K 报告 `AP50 = 95.2`、`mAP = 60.6`：

- He et al., **Spectral Discrepancy and Cross-modal Semantic Consistency Learning for Object Detection in Hyperspectral Images**, 2025  
  [arXiv](https://arxiv.org/abs/2512.18245)

跨论文数字不一定完全可比，但它揭示了一个对本赛很重要的现象：SDCM 的 AP50 更高，OSSDet 的 COCO mAP 略高。我们的唯一核心是后者，所以当前优先 OSSDet。

2026-07-23 最新发布的 S3GNet 也证明一阶光谱导数能减弱照明变化并保留材质差异，但其任务是显著性分割，不直接输出多类边界框，因此只能作为后续光谱输入消融的依据，不能替代主检测器：

- Peng et al., **Spectral-Spatial Synergistic Guided Network for Hyperspectral Salient Object Detection**, IEEE TMM / arXiv 2026  
  [arXiv](https://arxiv.org/abs/2607.21032)

### 5.2 模型结构

适配后的单模型命名为 `OSSDet-16`：

1. **16 波段输入与单流骨干**  
   完整光谱立方体直接进入 `in_channels=16` 的骨干网络，不压成伪 RGB，不先人为选 3 个波段。保留全部波段，避免丢失真实/塑料/木材之间的材料差异。

2. **CSSP：级联光谱—空间联合感知**  
   在高层特征上分别形成光谱注意和空间注意，再进行双向交互调制，让目标区域的空间位置和波段响应相互校正。

3. **SACF：光谱引导的自适应跨层融合**  
   其 SFA 子模块在局部邻域聚合相似光谱，强化同一目标内部的一致性；SDE 子模块分离并增强高频空间细节，补偿下采样带来的边界损失。它直接对应本赛“材质易混 + 高 IoU 精确框”的双重难点。

4. **目标感知掩码分支**  
   根据训练框生成高斯软目标图，显式监督网络激活目标并抑制背景。论文的推荐起点是背景平衡系数 `γ=0.1`、辅助损失权重 `α=0.6`。

5. **CAFR：跨光谱注意特征精修**  
   将低层目标线索自底向上传回多尺度特征，缓解不同层之间的光谱偏差。

6. **水平框检测头**  
   保留论文“候选点初始化 + 边界精修 + IoU 类损失”的思想，输出本赛要求的水平框。最终头部必须按官方 COCO evaluator 验证，不能直接照搬 MODA 的旋转框提交格式。

这仍是一个端到端单模型；掩码分支只是训练时辅助监督，不是第二个检测模型。

### 5.3 为什么它可能特别适合本赛

- 真实/仿制类别外观接近，OSSDet 保留完整波段并做光谱注意；
- 小目标和遮挡需要跨层细节，SACF/SDE 正是为下采样细节损失设计；
- 背景干扰可能造成误检，目标激活分支直接抑制背景；
- AP75–AP95 要求精确框，空间细节增强和框精修比单纯提高分类置信度更有价值；
- 只有一个模型、一次主干前向，符合单模型规则，也便于赛后复现。

---

## 6. OSSDet-16 的初始落地草案

### 6.1 数据与坐标

1. 原始 PNG 只用官方 `X2Cube` 解码；
2. 对每张图核对 `cube.shape[:2] == XML 中 (height, width)`；
3. 只用训练集计算每波段稳健统计量；
4. 采用训练集全局的逐波段 percentile clipping + mean/std，避免逐图 min-max 抹掉材质绝对响应；
5. 保存原始尺寸、缩放比、padding，推理框精确逆变换；
6. 做单元测试覆盖边界框缩放、翻转、裁剪和 CSV 序列化。

### 6.2 无泄漏验证

随机切图很可能把相同场景/背景分到训练和验证，导致虚高。建议：

1. 用训练图的背景特征或感知哈希聚类近似拍摄场景；
2. 按场景分组，再兼顾类别和目标尺寸做 5-fold；
3. 固定一折可用于工程诊断和训练曲线观测，但短训不得判定架构上限；
4. 进入候选终局的改动，检查 5 折均值、标准差及各折方向；
5. 最终不融合 5 折模型，只按已确定配置在全部训练集上训练一个模型。

保留一个改动的标准：主指标提升应超过折间噪声，并且多数折同方向；不能因为某一折或一次公开榜上涨就写入最终方案。

### 6.3 训练起点

- 骨干：ResNet-50/论文开源实现，`in_channels=16`；
- 初始化：规则未确认外部预训练前，随机初始化；可先仅用竞赛训练图做自监督预训练，再微调检测；
- 输入：保持长宽比，先测原生约 512×256，再测 2× 分辨率；不盲目拉成正方形；
- 多尺度特征：优先检查 stride-4/P2 检测层对小目标 AP 的增益；
- 损失：Focal/Quality Focal + GIoU/Convex GIoU + 框精修 + `0.6 * L_act` 为起点；
- 优化：AdamW 或论文配置作基线，warmup + cosine，梯度裁剪；
- 单一最终权重：可使用训练期 EMA 后的一个 checkpoint；
- 类别平衡：repeat-factor sampler 或 class-aware sampler，重点看分类型 AP；
- 增强：水平/垂直翻转、适度缩放平移、保长宽比裁剪、整谱一致的曝光变化；
- 禁止把 RGB 的 hue/channel shuffle 直接作用到 16 个有物理顺序的波段；
- Mosaic/MixUp 只有在 OOF 主指标稳定提升时保留，且所有 16 个波段必须同步变换。

一阶光谱导数、平滑波长扰动可作为后续单模型输入消融，但不能一开始同时堆入，避免无法判断收益来源。

### 6.4 长程训练与可信收敛

这是所有模型训练的最高优先级规则：

1. **按 optimizer updates 设计，不按任意 epoch 设计。** 先用训练集大小和
   全局 batch 计算每轮更新数，再确定足够长的优化跨度。其他数据集论文中的
   epoch 数不能直接照搬。
2. **Smoke 与正式训练严格分工。** Smoke 只验证读取、坐标、显存、DDP、反向、
   EMA、checkpoint 与 CSV 链路；它不回答模型能达到多少 AP。
3. **高潜力模型直接进入完整长程训练。** 从开始就给出高软上限、连续 LR、
   合理增强阶段、EMA、定期验证和可恢复 checkpoint，不设置 36/60/96 这种
   缺少收敛依据的逐级门槛。
4. **软上限不是模型上限。** 到达计划尾部仍刷新最佳分数时，从同一 checkpoint
   无缝延长低 LR 阶段，不重置优化器、不重做已经完成的训练。
5. **收敛必须有证据。** 只有主指标跨多个验证窗口进入平台、LR 已到低位、
   最佳点不在边界，且 AP75、小目标与弱类也不再上升时，才允许停止并判断
   架构。最终一轮仍是最佳即默认未收敛。
6. **公平比较。** 早期曲线只与相近 updates/样本曝光的模型比较；重大架构
   结论必须比较双方充分训练后的最佳 checkpoint。
7. **训练时间宝贵的正确含义。** 避免欠训练、碎片化长训和反复从头启动；
   不得为了表面节省数小时而增加整条路线被误判、重新训练数天的风险。

当前 V2 已把这套原则实现为绝对 optimizer-update 调度和可恢复的多指标平台
检测：从 3,924 updates 的旧 checkpoint 原地续接，完成多级 LR 退火后，只有
主 mAP、AP75、小目标和弱类在终端低 LR 下连续 20 次验证均无实质刷新，才允许
标记收敛。100,000 updates 只是安全边界；边界耗尽仍在提升时必须延长同一
checkpoint。具体配置、阶段和启动命令见
`V2高分方案实施说明.md`。

重大版本允许一次合并所有有论文、诊断或实现证据支持的高收益改动。可以用
低成本消融定位问题，但不得为了科研可归因性把一个应当完整训练的强方案拆成
多个保守初版。

#### 6.4.1 跨版本升级不得让 optimizer/EMA 失忆

V3 从 e228 使用 model-only `--tuning`，丢弃已经形成的 AdamW moments；V5
首次启动也曾错误地把 V4 e30 只当作模型初始化来源。后者在正式长训早期即被
停止，并改为完整 stateful 迁移。这两次事故确立以下永久规则：

1. `vN -> vN+1` 首先按“连续升级”设计，而不是默认 fresh fine-tune。
2. 对同坐标参数和可证明的代数重参数化，必须迁移 raw、EMA/年龄及 AdamW
   `step/exp_avg/exp_avg_sq`；语义克隆和真正新增参数必须分别列出。
3. 本地 LR、增强和结构过渡进度可以从零计数，但旧参数的 optimizer step 不得
   因此归零。warmup 和 transition 不是状态继承的替代品。
4. 正式训练前必须完成“父代 stateful 起点 -> 若干真实更新 -> 保存子代
   checkpoint -> 再恢复若干更新”的双段 smoke，并断言旧参数步数按实际更新数
   递增、新参数从零递增、EMA 与 checkpoint 恢复连续。
5. 如果结构变化使某些 moments 无法合理映射，必须在启动前向用户报告具体范围
   和风险并取得确认；不得静默丢弃，也不得只用“架构变了”作为笼统理由。
6. 语义克隆不等于梯度坐标等价。默认只克隆权重/EMA；AdamW moments 只有在
   坐标和尺度有充分依据时才迁移。当前 V5 的 15 个 salience moment clone 是
   已运行 lineage 的显式启发式例外，不是后续版本模板。
7. runner.iter 是 attempted iterations，AMP overflow 会跳过真正的 optimizer
   step。训练报告和停止判断必须同时给出 attempted、AdamW successful step 和
   skip 数；调度若按 runner.iter 前进也必须明确。
8. FP16 scaler 是独立状态。父代有 scaler 时必须记录父值，并明确选择继承或
   changed-graph recalibration；子代普通 resume 不允许 scaler 丢失。
9. 正式加载必须经过 fail-closed gate：模型/EMA name-shape-dtype、optimizer
   group/name/order/shape/超参数、finite moments、step 方程、lineage、scaler、
   checkpoint SHA、源码和上游版本任一不符即拒绝。不得依赖框架的非严格加载。
10. 未保存 RNG/worker/sampler 状态时只能称为 state-continuous，不能宣称
    bitwise resume；换卡数和参数组配置变化也必须重新审计。

V5 的修正实现以 V4 e30 的 `epoch=30, iter=42180` 为父状态：全部 1,015 个
父代 AdamW state 均被使用，V5 目标侧迁移 1,030 个 state，只有 60 个真正新增
的 FDR/LQE 张量 fresh；按可训练参数量计保留约 `99.73%` 的 optimizer 记忆。
该统计是本次实例，不是以后版本可照抄的常数；每一代都必须重新审计。

补充审计确认：V4 e30 的 `42180` 是 attempted iterations，全部 AdamW state
实际为 `step=42168`，日志对应 12 次 AMP skip。V4 checkpoint 也确实保存了
scale=8、growth tracker=1535 的 scaler。V5 bootstrap 选择从 scale=1 重新校准
是 changed-graph 的显式策略，不是“父代没有保存”；任何 V5 普通续训都必须恢复
子代 scaler。当前 V5 为兼容 lineage 保留 optimizer.step 前 EMA 和 transition
buffer 平滑，因此早期验证的结构过渡显著滞后；其修正必须放到新版本边界，禁止
训练中途暗改。

### 6.5 推理与合法后处理

模型能力迭代期间冻结为同一条冠军推理协议：

1. 加载本版本自己充分收敛的单一 EMA checkpoint；
2. 全图宽度 1280，分别执行 identity 与 horizontal 两视图；
3. 将翻转框精确映射回原图，clip 并删除退化框；
4. 同类别使用 Gaussian Soft-NMS
   (`iou_threshold=0.55`, `sigma=0.5`)；
5. 使用 `pre_score=output_score=0.0001`，每图全类别保留 top 300；
6. 生成连续唯一的 `id`，严格检查图片覆盖、类别和坐标。

V5/V6/V7/V8/... 晋级时不得同时改变该协议。只有模型端已经形成可信平台并暂时
冻结后，才重新研究多尺度/full TTA、Soup/SWA、切片、WBF、逐类阈值等常规
推理优化。总纲 2.4 的边界手段仍须用户逐项授权和规则专项复核。

### 6.6 按提分价值排序的消融

| 优先级 | 单变量实验 | 主要观察 |
|---:|---|---|
| 0 | 官方 evaluator + 坐标回环测试 | 排除“模型没问题、提交错了” |
| 1 | 普通 16 通道骨干 vs 完整 OSSDet 模块 | 验证论文迁移是否成立 |
| 2 | 原生分辨率 vs 2× 输入 | AP_small、AP75–95 |
| 3 | 是否加入 P2/stride-4 检测层 | 小目标召回与误检 |
| 4 | `γ`、`α` 和目标软掩码尺度 | 背景误检、遮挡、小目标 |
| 5 | 全局逐波段归一化与光照增强 | 真实/仿制类别混淆 |
| 6 | 类别均衡采样与 focal 参数 | 少数类 AP、总体 mAP |
| 7 | 框损失、质量感知分数 | AP75–95 与排序 |
| 8 | NMS、top-k、score threshold | OOF 总 AP 与召回 |

低成本诊断可以一次只改变一个因素；重大版本可以合并所有有强证据的高收益
改动，不为科研归因性拆成多个保守长训。所有运行仍须保存配置、optimizer
updates、日志、OOF 预测和 per-class AP。每天最多 3 次榜单提交，应把 Kaggle
提交当作稀缺实验资源。

---

## 7. 比赛节奏

### Phase 1 前半段：建立可信基线

- 下载后完成数据审计、类别/框统计和可视化；
- 复刻官方指标；
- 跑通普通 16 通道检测基线；
- 跑通 OSSDet-16；
- 建立固定场景分组 CV；
- 每次 Kaggle 提交只验证一个关键变化。

### Phase 1 后半段：挤主指标

- 聚焦低 AP 类、真实/仿制混淆和小目标；
- 完成分辨率、P2、损失、采样、NMS 消融；
- 锁定最终单模型配置、以 optimizer updates 表示的长程调度和收敛停止规则；
- 预演从权重到 CSV 的完整可复现流程；
- 提前准备 Phase 2 一键批量推理脚本。

### 当前模型迭代窗口：V5/V6/V7/V8/...

- 连续推进具有模型级提升潜力的重大版本，每个版本完整长训至可信收敛；
- 所有版本使用冻结的冠军推理协议，避免后处理掩盖模型真实进步；
- `0.70+` 公榜为近期阶段里程碑，不写成收益上限或自动停止条件；
- 只有连续重大版本不再产生可靠增益时，才暂时停止模型更新并观察排行榜；
- 模型冻结后先研究常规合规后处理；只有其也形成平台，才可能重新评估仍冻结的
  合规边界储备。

### Phase 2：稳，不临时发明新模型

- ranking 发布后立即校验文件数、尺寸和读取；
- 使用已锁定的单模型分别推理 test 与 ranking；
- 合并成一个完整 CSV；
- 做行数、图片覆盖、类别范围、坐标范围、NaN/Inf、重复 `id` 检查；
- 先提交最稳健版本，避免并列时吃亏；
- 剩余提交只用于已由 OOF 支持的阈值/NMS修正；
- 不根据公开的 100% test 临时过拟合，因为真正决定胜负的 ranking 仍完全私有。

---

## 8. 当前待确认清单

数据下载后必须立即补齐：

- 官方 sample submission 的空图像处理方式；
- 自定义 COCO metric 的 `maxDets`、坐标端点和无预测图像处理；
- 所有训练图的真实尺寸、实例分布、重复场景和异常 XML；
- 是否存在无目标训练图；
- 训练/测试文件名是否可能重复；
- Kaggle 页面倒计时与规则日期是否一致。

需要在 Kaggle 公共讨论区向主办方确认：

- 哪些外部预训练权重允许、如何声明；
- 是否允许 test/ranking 无标注自监督或伪标签；
- 同模型 TTA 是否被单模型规则允许。

在确认之前，不把这些高风险手段纳入最终提交。

---

## 9. 胜负判断标准

最终只问五个问题：

1. 这个改动是否提高无泄漏 OOF `mAP@[.50:.95]`？
2. 提升是否跨折稳定，而不是随机种子或单一场景噪声？
3. 它是否改善 AP75–95，而不只是 AP50？
4. 它能否由一个模型、一个权重、一条命令完整复现？
5. 它是否明确符合竞赛规则？

五项全部为“是”，才进入最终方案。

---

## 10. 参考来源

竞赛事实以 2026-07-25 读取的官方页面为准：

1. [Kaggle Competition Overview](https://www.kaggle.com/competitions/hyperspectral-object-detection-challenge-2026/overview)
2. [Kaggle Data Description](https://www.kaggle.com/competitions/hyperspectral-object-detection-challenge-2026/data)
3. [Kaggle Official Rules](https://www.kaggle.com/competitions/hyperspectral-object-detection-challenge-2026/rules)
4. [Han et al., MODA / OSSDet, AAAI 2026](https://doi.org/10.1609/aaai.v40i6.42457)
5. [OSSDet Official Code](https://github.com/shuaihao-han/MODA)
6. [He et al., SDCM, arXiv:2512.18245](https://arxiv.org/abs/2512.18245)
7. [Peng et al., S3GNet, arXiv:2607.21032](https://arxiv.org/abs/2607.21032)

> 若 Kaggle 官方页面后续更新，规则、时间和数据说明一律以最新官方内容覆盖本文；技术路线仍只以合法、可复现的私榜主指标为准。
