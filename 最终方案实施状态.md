# HOD Challenge 2026 最终方案实施状态

> 2026-08-08 更新：V5R e40 的 Fold0 macro 为 `0.717700`，但在与 V4 完全
> 相同的冻结推理协议下公榜只有 `0.66977`，低于 V4 的 `0.67686`，因此 V5/V5R
> 已淘汰。当前已切到独立分支 `agent/v6-independent`，V6 从唯一公开 Co-DINO
> ViT-L checkpoint 开始，不继承 V4/V5/V5R 权重；完整证据与架构见
> [V5 全量诊断与 V6 独立方案](docs/v6_diagnosis_and_design.md)。下文早期模型内容
> 继续保留作历史审计，不代表当前待实现架构。

> 2026-07-30 更新：当前公榜冠军已经升级为 V4 Co-DINO ViT-L epoch-30 EMA
> 单 checkpoint，采用固定的 1280 identity + horizontal 两视图、同类 Gaussian
> Soft-NMS (`iou_threshold=0.55`, `sigma=0.5`,
> `pre_score=output_score=0.0001`) 和每图全类别 top 300。Fold0
> `mAP@[.50:.95]=0.71711748`，公榜 `0.67686`。V5/V6/V7/V8/... 使用各自
> 收敛 EMA 权重但沿用同一推理协议，先持续提升模型能力；`0.70+` 为阶段
> 里程碑而非停止上限。本文其余 V1/V2 内容保留作历史审计，当前最高优先级以
> [AGENTS.md](AGENTS.md) 和
> [高光谱目标检测竞赛作战总纲.md](高光谱目标检测竞赛作战总纲.md) 为准。

> 2026-07-26 更新：V1 全量 120 轮公榜仅从 `0.59053` 到 `0.59147`，
> 已判定为无效增益。当前主线升级为 V2
> **Objects365→COCO DEIM-D-FINE-X + 16 波段适配与光谱残差**。
> 最新架构、门槛和命令以
> [V2高分方案实施说明.md](V2高分方案实施说明.md) 为准；下文保留 V1
> 实施记录用于审计，不再代表当前主模型。

> 更新：2026-07-25  
> 唯一优化目标：在遵守竞赛规则的前提下，最大化私榜
> `mAP@[0.50:0.95]`。AP50 仅作诊断，不作为选模依据。

## 1. 我们在做什么，最后交什么

输入是 16 位单通道 4×4 光谱马赛克。流水线将其严格解码为 16 波段
高光谱立方体，训练一个单一目标检测模型，预测 18 类目标的类别、置信度
和水平框。

最终 Kaggle 文件固定为：

```csv
id,image_id,class_id,confidence,x1,y1,x2,y2
0,1000,2,0.95000000,186.0000,28.0000,201.0000,50.0000
```

- `id` 从 0 连续编号；
- `image_id` 是 PNG 文件名去后缀；
- `class_id` 为 0–17；
- 坐标位于 X2Cube 解码后的空间尺寸中；
- Phase 1 提交 1000 张 test；
- 2026-09-23 发布 ranking 图后，最终 CSV 必须同时覆盖 test 和 ranking。

Phase 1 公榜只评随机 50% test；最终名次不是现在的公榜名次。2026-07-25
可检索的 Kaggle 页面状态为 15 entrants、0 submissions。

## 2. 官方数据对齐结果

官方原始数据保存在 `data/raw/`，从未改写。派生缓存放在大盘
`/data/xuhl-competition-2026/cache/`。

| 项目 | 结果 |
|---|---:|
| 训练图 | 3000 |
| Phase 1 测试图 | 1000 |
| 清洗后目标框 | 10066 |
| 类别 | 18 |
| 官方异常文件 | 12 |
| 五折图像数 | 600 / 601 / 602 / 599 / 598 |

处理原则：

1. 完全按 4×4 位置顺序执行 X2Cube，输出 `(H/4, W/4, 16)`；
2. 41 个退化框只在训练清单中剔除，不修改 XML；
3. 3 个越界框裁剪到实际 cube 边界；
4. 3 个 XML 尺寸不一致文件以 PNG 实际解码尺寸为准并记录审计；
5. 16 波段归一化统计只使用训练集；
6. 五折按多标签实例数平衡，并把感知哈希近重复场景绑定到同一折。

完整机器可读报告：

```text
storage/cache/data_alignment_report.json
```

## 3. 已部署的最终候选模型

模型是一个约 28.43M 参数的单检测器，不做多模型集成：

```text
16-band cube
  ├─ HYPER-DET SSRM → COCO-pretrained YOLO11l RGB-compatible branch
  └─ 3-D spectral-spatial HSI branch
       ↓
  HUFF fusion at P2 / P3 / P4 / P5
       ↓
  OSSDet-style object-aware auxiliary supervision at P2
       ↓
  P2–P5 FPN/PAN + Task-Aligned DFL detection head
       ↓
  one set of boxes, classes and confidence scores
```

依据：

- 2026 年直接面向 HOD3K 的
  [HYPER-DET / HUFF](https://doi.org/10.1109/JSTARS.2026.3695926)
  负责双流光谱—空间提取、SSRM 和 homogeneous/unique 融合；
- AAAI 2026 正式发表的
  [OSSDet](https://ojs.aaai.org/index.php/AAAI/article/view/42457)
  提供目标感知抑制背景的辅助监督；
- 四尺度 P2 检测头针对本数据大量小框；训练框宽度中位数仅 24 像素，
  5% 分位数为 13 像素；
- DFL 与任务对齐分配服务于高 IoU 定位，直接针对主指标的
  AP75–AP95，而非只追 AP50。

公开 COCO 权重只初始化 RGB-compatible 分支，高光谱分支、融合模块、
目标感知分支和 18 类输出从头训练。来源与 SHA-256 已记录在：

```text
storage/pretrained/yolo11l.pt.provenance.json
```

该外部预训练必须随最终方案显式声明。

## 4. 训练与验证策略

- fold0：2400 张训练、600 张无泄漏验证；
- 主选模指标：COCO `mAP@[.50:.95]`；
- 同时记录 AP50、AP75、尺寸 AP、每类 AP；
- AdamW、5 epoch warmup、cosine decay、AMP、梯度裁剪；
- 多尺度宽度 640/704/768/832，保持约 2:1 高宽比；
- 光谱增益、光谱斜率、噪声、band dropout，以及轻量空间仿射；
- EMA 使用 warm-ramp，避免早期权重被初始化状态污染；
- 单卡自动梯度累积 2 次，双卡累积 1 次，有效全局 batch 始终为 36；
- GPU0 空闲后可从整轮 `last.pt` 恢复为双卡 DDP，不改变优化语义。

RTX 4090 实测安全上限采用每卡 batch 18：在最宽 832 输入、包含模型、
AdamW 状态、EMA 和一次完整反传时，峰值 20.35 GiB、保留显存
20.72 GiB，仍留约 3.8 GiB 防止批次目标数波动导致 OOM。

fold0 用于选择训练轮数、后处理阈值和合法的单模型推理策略。确定后，
用全部 3000 张训练图重新训练一个最终权重；最终 CSV 只来自这一个权重。

## 5. 当前运行状态与路径

长时间训练已按要求停止，当前没有占用 GPU1。最近一次短 run 在第 5 轮
中止，保留到第 4 轮的 `last.pt` 和完整日志，仅供链路审计，不作为最终
模型：

```text
storage/runs/hyperdet_huff_p2_l_fold0_20260725_161001/
storage/checkpoints/hyperdet_huff_p2_l_fold0_20260725_161001/
storage/logs/fold0_single_ema_ramp_20260725_1612.log
```

代码测试：

```text
7 passed
```

覆盖 X2Cube、异常 XML、坐标往返、五折平衡、模型前向与反传、EMA
以及带 `id` 的 CSV。

## 6. 复现命令

```bash
source .venv/bin/activate
hod26-prepare --config configs/final.yaml

# 当前单卡（建议用新 batch/LR 从头启动）
CUDA_VISIBLE_DEVICES=1 PYTHONUNBUFFERED=1 \
  hod26-train --config configs/final.yaml

# GPU0 空闲后，从整轮 checkpoint 切换双卡
CUDA_VISIBLE_DEVICES=0,1 PYTHONUNBUFFERED=1 \
  .venv/bin/python -m torch.distributed.run \
  --standalone --nproc_per_node=2 \
  -m hod26.train --config configs/final.yaml \
  --resume storage/checkpoints/<run>/last.pt

# 验证与最终 CSV
hod26-validate --config configs/final.yaml --checkpoint <best.pt>
hod26-infer --config configs/final.yaml --checkpoint <final.pt>
hod26-submit-check --config configs/final.yaml \
  --csv storage/submissions/submission.csv
```

## 7. 合规边界

已执行的提分手段都在单模型和官方数据范围内。当前未启用：

- 多模型/多折预测融合；
- 外部未声明数据；
- 人工测试标注或榜单探测；
- test/ranking 伪标签；
- 多尺度/full TTA（冻结冠军协议中的单次水平翻转除外）。

其中多模型集成和作弊明确不做。当前仅冻结采用同一 checkpoint 的 1280
identity + horizontal 两视图冠军协议；多尺度/full TTA 及无标注测试自训练等
不自动启用，必须保留规则依据、单模型解释和完整审计。
