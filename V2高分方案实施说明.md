# HOD26 V2 高分方案实施说明

> 日期：2026-07-26  
> 唯一目标：在不作弊、保持最终单模型的前提下，最大化 Kaggle 私榜
> `mAP@[.50:.95]`。`+0.03～0.05` 公榜绝对提升只是重大版本的最低验收线，
> 不是目标上限。

## 结论

V2 相比当前 V1 是结构级升级，不是训练轮数或阈值微调：

```text
16-band X2Cube
  ├─ learnable 16→3 pretrained-compatible proxy
  │      └─ HGNetv2-B5 (official Objects365→COCO weights)
  └─ native 16-band spectral pyramid
           ↓ zero-initialized gated residual fusion at stride 8/16/32
       DEIM Dense O2O + MAL matching
           ↓
       D-FINE FDR + GO-LSD decoder
           ↓
       one model / 18 classes / one set of boxes
```

它更强的核心不是参数更多，而是直接针对 Fold0 已确认的主损失：

- 分类错误在 IoU50 下几乎已经解决，继续堆分类收益很低；
- IoU75 下的定位损失、重复框、objectness 排序和小目标才是主要失分；
- D-FINE 的细粒度分布精修服务于 AP75–AP95；
- DEIM 的 Dense O2O/MAL 改善稠密监督、匹配和分数排序；
- 输入从 V1 的最高 832 宽提升到 1024×512，直接增加小框的有效像素；
- 预训练主干保持完整，新增光谱路径以零门控接入，训练起点不会被随机
  光谱分支破坏。

## 主模型选择

本轮重新核查了截至 2026-07-26 可用的官方实现和权重：

| 候选 | 官方 COCO AP | 本项目判断 |
|---|---:|---|
| EdgeCrafter-X | 57.9 | 更偏效率，精度低于本方案权重 |
| DEIMv2-X | 57.8 | 更新、更轻，但公开闭集权重低于本方案 |
| RF-DETR-L | 56.5 | 开源版本较低；60.1 的 2XL 是 Plus/PML 组件 |
| DEIM-D-FINE-X COCO | 56.5 | 可用，但不是最强初始化 |
| **Objects365→COCO DEIM-D-FINE-X** | **59.5** | **V2 主核** |
| Stable-DINO / Co-DETR 大模型 | 64–66 | 参数与训练显存大幅上升，16 波段高分辨率迁移风险高；保留为 V2 未过门槛后的下一条结构路线 |

因此“最强”按本赛预期收益、可获得的完整权重、两张 4090 可训练性和
定位误差匹配度综合选择，不按单一 COCO 数字选一个无法可靠落地的模型。

论文与官方实现：

- [D-FINE](https://arxiv.org/abs/2410.13842)
- [DEIM](https://arxiv.org/abs/2412.04234)
- [官方 DEIM 代码与权重](https://github.com/Intellindust-AI-Lab/DEIM)
- [DEIMv2](https://arxiv.org/abs/2509.20787)
- [EdgeCrafter](https://arxiv.org/abs/2603.18739)
- [RF-DETR](https://arxiv.org/abs/2511.09554)

## 已落地状态

- 官方 DEIM 固定提交：
  `09d35d53d39ee3145a1e61e3a989b28b9468d1dd`
- 官方 59.5 AP 权重：
  `storage/pretrained/v2/deim_dfine_x_object365.pth`
- 权重 SHA-256：
  `f240a1128280eb6ce0fff911e8811a8dff669e18be25f5160db7b4263452277d`
- Fold0 数据：2400 train / 600 validation；
- 完整加载官方张量 1424 个，语义迁移 80→18 分类头张量 15 个；
- 模型参数量：63,607,580；
- 双卡配置：每卡 batch 11，全局 batch 22，1024×512；
- 单卡最大分辨率完整优化步峰值：21.0 GiB；
- 单卡、双卡 DDP、反向、EMA、评估、checkpoint 和最终 CSV 校验均已通过；
- 已完成 36 轮早期训练记录，没有提交 Kaggle；该记录未收敛，不代表模型上限。

预训练权重来源、哈希和用途已生成机器可读声明：

```text
storage/pretrained/v2/deim_dfine_x_object365.pth.provenance.json
```

## 36 轮结果、错误解释与永久纠正

当前 V1 的无泄漏 Fold0 基线约为：

```text
mAP@[.50:.95]  0.6978
AP75            0.832
small AP        0.668
domain-hard AP  0.6868
弱四类均值       约 0.446
```

36 轮运行于 2026-07-26 正常完成，共 3,924 次 optimizer updates：

```text
mAP@[.50:.95]  0.69607
AP50            0.94397
AP75            0.82378
small AP        0.66687
AR100           0.77965
```

第 36 轮是全程最佳，24→36 轮仍增加约 `+0.0158`，训练 loss 仍下降。
因此这次训练**没有收敛**。它只能证明：

- 完整数据、预训练迁移、双卡训练、EMA、评估和 checkpoint 链路工作正常；
- V2 用约 3,924 次 updates 已达到 V1 约 6,700 次 updates 的 Fold0 水平，
  显示出更快的优化速度；
- 目前无法知道 V2 的充分训练上限。

原“epoch 24/36 达不到指定 AP 就淘汰、通过后再重头训练 96 轮”的流程作废。
这是把工程 smoke、早期曲线和收敛后的模型判断混为一谈，会制造欠训练误判与
多次重头训练，实际更加浪费时间。

## 正式长程训练原则

- 不再做 36→60→96 的碎片化晋级，也不把 96 当作模型极限；
- 预算以 optimizer updates 为主。初始长程调度必须覆盖数万次 updates，
  而不是数千次，并设置足够高的软上限；
- 学习率、增强和 EMA 从开始到长尾必须是一条连续计划；checkpoint 必须保存
  模型、EMA、优化器、调度器和 epoch，使 horizon 不足时能从原状态直接扩展；
- 只有当 mAP、AP75、small AP 和弱类 AP 跨多个评估窗口进入平台，学习率已到
  低位，且最佳点不在最后一次评估时，才判定收敛；
- 到达软上限仍在刷新最佳时，延长同一训练的低学习率尾段，不从头重跑；
- 只有充分训练后的本地结构性提升支持至少 `+0.03～0.05` 公榜绝对提升时才
  消耗提交机会；这是最低验收线，不是目标上限。

## 已落地的 V2 上限测定

正式配置为 `configs/v2/deim_dfine_x_hsi_long.yaml`。它直接承接 36 轮
`last.pth`，把该 checkpoint 视为一条长训练的第 3,924 次更新，不重新初始化
模型、EMA 或优化器。

学习率按**绝对 optimizer update**连续调度：

| updates | 基础 LR 比例 | 用途 |
|---:|---:|---|
| 3,924→16,000 | 0.50 | 在可控 LR 下完成充分特征探索 |
| 16,000→32,000 | cosine 0.50→0.10 | 长程主退火 |
| 32,000→42,000 | cosine 0.10→0.03 | 定位精修 |
| 42,000→50,000 | cosine 0.03→0.01 | 进入极低 LR |
| 50,000 以后 | 0.01 | 只用于确认真实平台，而非追赶固定轮数 |

增强会在续训时重新进入强探索：MixUp 到 epoch 180，光谱增强、Mosaic、
IoU crop 和多尺度到 epoch 300；之后保持干净输入完成长尾精修。这里的
`epoches: 1000` 只是循环防护，不是训练目标。

每 4 轮评估一次，约等于每 436 updates 一次。只有同时满足以下条件才会自动
标记 `credible_multimetric_convergence`：

1. 已达到至少 55,000 updates；
2. LR 已经处于不高于 0.011 倍的终端段；
3. 在终端 LR 下至少连续评估 20 次；
4. `mAP@[.50:.95]`、AP75、small AP、弱四类 AP 都没有达到各自的实质刷新
   阈值。

因此正常情况下至少要在 50,000 updates 之后再观察约 8,700 updates 的低 LR
尾段。`100,000 updates` 是防失控安全边界；若到那里仍未满足收敛证据，程序会
明确写出 `safety_update_ceiling` 和 `is_model_converged: false`，此时只扩展
同一 checkpoint，不得据此改模型。

按 36 轮实测速度估算，最早形成可信收敛证据约还需 16～20 小时；若曲线持续
提升而运行到 100,000-update 安全边界，约需 28～34 小时。实际以日志中的
updates/秒和评估耗时为准。

## 手动命令

下列 36 轮命令仅保留为历史审计，**不得再用于判断 V2 上限**：

```bash
cd /home/user/桌面/xuhl-competition
tools/train_v2_pilot.sh 2>&1 | tee \
  storage/v2/runs/deim_dfine_x_hsi_fold0_pilot.console.log
```

正式长程由用户手动启动：

```bash
cd /home/user/桌面/xuhl-competition
mkdir -p storage/v2/long_runs
tools/train_v2_long.sh 2>&1 | tee \
  storage/v2/long_runs/deim_dfine_x_hsi_fold0_long.console.log
```

启动器优先恢复长程目录自己的 `last.pth`；第一次运行才自动恢复 36 轮 pilot
的 `last.pth`。所有新 checkpoint 都持久化 update 计数、全局最佳值、多指标
平台状态和当前 LR 比例。中断后重复同一命令即可无缝继续。

若安全边界耗尽但日志明确显示尚未收敛，保留原调度并只延长未来边界：

```bash
tools/train_v2_long.sh \
  --update max_optimizer_updates=140000 2>&1 | tee -a \
  storage/v2/long_runs/deim_dfine_x_hsi_fold0_long.console.log
```

36 轮最佳权重继续保留作审计。在正式长程满足收敛规则、完成逐类诊断并证明
结构性大幅提升之前，不生成或提交新的 Kaggle 候选 CSV。

## 合规边界

- 最终只保留一个模型权重，不融合不同模型或不同 fold；
- 不使用测试集人工标注、榜单探测、伪标签或自训练；
- 外部预训练完整声明，用户另行向主办方确认；
- 同模型 TTA/分块推理暂不写死，等 Fold0 用同一权重完成合法离线对比后再决定；
- Kaggle CSV 只在用户明确要求时上传。
